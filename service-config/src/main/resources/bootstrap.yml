spring:
  application:
    name: service-config
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        serviceId: config
    bus:
      enabled: true
      trace:
        enabled: true
  rabbitmq:
    host: 10.24.0.118
    port: 5672
    username: guest
    password: guest
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
server:
  port: 8882

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
